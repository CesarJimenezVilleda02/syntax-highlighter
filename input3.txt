#include <stdio.h>
int main() {  
    char c;
    printf("Enter a character: ");
    scanf("%c", &c);  
    
    // %d displays the integer value of a character
    // %c displays the actual character
    printf("ASCII value of %c = %d", c, c);
    
    return 0;

    b=7

    int a = 32.4 *(-8.6 - b)/       6.1E-8

    d = a ^ b //Esto es un comentario

    for (int i = 0; i < size; i++) {
            i++;
    }

    int var_jaja;
    while(var_jaja > 8) {
        // esto es un comentario
        var_jaja++;

    }

    // un nuevo comentario
    printf("HOA MUNDO");

    char mivar = "hola soy cesar y fer";

    int menu_option;
    do {
        printf("Enter an option:\n0. Quit\n1. Play Game\n");
        scanf(menu_option);
        switch(menu_option) {
            case 0:
                println("Has salido del juego\n");
                break; //esto quita el programa
            case 1:
                play_game();
                break;
        }
    } while(menu_option);
}
#include <stdio.h>
int main() {  
    char c;
    printf("Enter a character: ");
    scanf("%c", &c);  
    
    // %d displays the integer value of a character
    // %c displays the actual character
    printf("ASCII value of %c = %d", c, c);
    
    return 0;

    b=7

    int a = 32.4 *(-8.6 - b)/       6.1E-8

    d = a ^ b //Esto es un comentario

    for (int i = 0; i < size; i++) {
            i++;
    }

    int var_jaja;
    while(var_jaja > 8) {
        // esto es un comentario
        var_jaja++;

    }

    // un nuevo comentario
    printf("HOA MUNDO");

    char mivar = "hola soy cesar y fer";

    int menu_option;
    do {
        printf("Enter an option:\n0. Quit\n1. Play Game\n");
        scanf(menu_option);
        switch(menu_option) {
            case 0:
                println("Has salido del juego\n");
                break; //esto quita el programa
            case 1:
                play_game();
                break;
        }
    } while(menu_option);
}
#include <stdio.h>
int main() {  
    char c;
    printf("Enter a character: ");
    scanf("%c", &c);  
    
    // %d displays the integer value of a character
    // %c displays the actual character
    printf("ASCII value of %c = %d", c, c);
    
    return 0;

    b=7

    int a = 32.4 *(-8.6 - b)/       6.1E-8

    d = a ^ b //Esto es un comentario

    for (int i = 0; i < size; i++) {
            i++;
    }

    int var_jaja;
    while(var_jaja > 8) {
        // esto es un comentario
        var_jaja++;

    }

    // un nuevo comentario
    printf("HOA MUNDO");

    char mivar = "hola soy cesar y fer";

    int menu_option;
    do {
        printf("Enter an option:\n0. Quit\n1. Play Game\n");
        scanf(menu_option);
        switch(menu_option) {
            case 0:
                println("Has salido del juego\n");
                break; //esto quita el programa
            case 1:
                play_game();
                break;
        }
    } while(menu_option);
}